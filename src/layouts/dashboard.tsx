// Deprecated: layout file previously named "dashboard.tsx". Kept for backward compatibility.
// Use the clearer filename `dashboard-layout.tsx` instead.

export { default } from "./dashboard-layout";
